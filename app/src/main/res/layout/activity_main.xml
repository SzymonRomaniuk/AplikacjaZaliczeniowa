<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Lokalizacja"
    android:id="@+id/przycisk"
    android:onClick="goToLokalizacja"
    android:layout_marginLeft="136dp"

    android:layout_marginStart="38dp"
        android:layout_marginTop="46dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Data i Czas"
        android:id="@+id/przycisk1"
        android:onClick="goToTemperatura"


        android:layout_alignBaseline="@+id/przycisk"
        android:layout_alignBottom="@+id/przycisk"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="32dp" />







</RelativeLayout>